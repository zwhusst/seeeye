/**
 * [Component] Setting
 */
function activate(employeeid) {
	_log("[func] activate: " + employeeId);
}

function setcap(employeeid) {
	_log("[func] setcap: " + employeeId);
}

function deactivate(employeeid) {
	_log("[func] deactivate: " + employeeId);
}

function view(rowno) {
	_log("[func] view: " + rowno);
}